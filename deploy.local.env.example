# Copy to deploy.local.env and fill in (deploy.local.env is gitignored).

# --- Dev (dev.akademiata.pl) ---
SFTP_HOST=dev.akademiata.pl
SFTP_PORT=22
SFTP_USER=your_sftp_username
SFTP_REMOTE_PATH=wp-content/themes/akademiata
SFTP_USE_PASSWORD=true
SFTP_PASSWORD=your_sftp_password
# SFTP_PRIVATE_KEY=C:\Users\Ata\.ssh\id_rsa

# --- Production (akademiata.pl) — required for npm run deploy:prod /deploy-prod ---
SFTP_PROD_HOST=akademiata.pl
SFTP_PROD_PORT=22
SFTP_PROD_USER=your_prod_sftp_username
SFTP_PROD_REMOTE_PATH=wp-content/themes/akademiata
SFTP_PROD_USE_PASSWORD=true
SFTP_PROD_PASSWORD=your_prod_sftp_password
# Omit SFTP_PROD_USER / SFTP_PROD_PASSWORD to fall back to dev SFTP_* values.

# Optional
# SKIP_BUILD=true
# DRY_RUN=true
